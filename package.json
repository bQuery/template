{
  "name": "bquery-template",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A template repository built with @bquery/bquery to build dynamic client-side apps",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@bquery/bquery": "^1.3.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
